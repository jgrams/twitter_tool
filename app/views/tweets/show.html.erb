<!DOCTYPE html>
<html>
  <head>
    <title>Twitter Cloud</title>
    <%= form_for @search do |s| %>
      <%= s %>
    <%= end %>
  </head>
  <div class="center">
    <% if current_user %>
      <%= form_for @new_search do |s| %> 
      <%= s.text_field :username, class: "search-bar", id: 'Search Bar' %>
      <%= s.submit "Find Someone Else", class: "button hover_button", id: 'Submit Search' %>
      <% end %>
    <% end %>
  </div>
  <body>
    <!-- You should explicitly specify the dimensions of the container element -->
    <div id="cloud" style="width: 550px; height: 350px; align-content: center; margin:0 auto; margin-top:100px"></div>
  </body>
</html>